name: Generar páginas de proyectos

on:
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - "datos/datos.yaml"
      - "scripts/generar-proyectos.js"
      - "scripts/lib/**"
      - "lib/js-yaml.min.js"

permissions:
  contents: write

jobs:
  generar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Hornear inicio, tienda, proyectos/index.html y proyectos/*/index.html desde datos/datos.yaml
        run: node scripts/generar-proyectos.js

      - name: Commitear páginas generadas
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html store proyectos
          if git diff --cached --quiet; then
            echo "Sin cambios que commitear."
          else
            git commit -m "Generar páginas de proyectos automáticamente"
            git push
          fi
